# extensiontest-sandbox

Tiny JS sandbox repo to generate realistic diffs for testing tooling (e.g. commit-summary extensions).

## Commands

- `npm test`
- `npm run smoke`

Includes a tiny retry helper around HTTP calls for noisier diffs.

