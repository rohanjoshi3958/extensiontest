{
  "name": "extensiontest-sandbox",
  "description": "Throwaway module for tooling experiments",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test src/**/*.test.js"
  }
}
